import TransferForm from './src/transfer-form.vue';

/* istanbul ignore next */
TransferForm.install = function(Vue) {
  Vue.component(TransferForm.name, TransferForm);
};

export default TransferForm;
